# G2 closure（2026-09-19）

更新：2026-09-19（Asia/Taipei）。Coordinator 依新候選與 Luna independent review 完成 G2 foundation scope 核准。這份 closure 只宣告共用 Shell、Full/Lite page lifecycle、C5/H5 split evidence 與 X1 page-channel scope 的 G2 結果；不宣告 G5、真實 FH6、完整 native first-paint 或效能完成。

## 固定候選與檢查

- Product base：`main@568da2041e0cb4bbb58583c3a4dc9a279508094d`。
- Integrated aggregate：`c38f13ec90e6de671bea51144446720c461650aa`。
- Shell candidate：`ee0f7bb9f5e607a682a5136bb0785deb580e51fb`。
- Shell：120 files／859 tests、build PASS；Full/Lite/sidecar 11.45.18 build PASS；executable bundle 2 tests PASS；backend 348 passed／8 deselected，Ruff check/format 與 version PASS。
- Exact product-head CI：#345 `ee0f7bb9...` 14 checks SUCCESS；#340–#344 exact heads 亦 SUCCESS。#339 的本次 plan documentation head 需在 root push 後重查自身 CI。

## C5 combined evidence

Browser Full/Lite actual `App`/`LiteApp` 使用 real `main`/`lite-main` StrictMode mount。非突變 observer 對 dark/light × default/modern/elegant 共 12 組記錄 first non-empty root 早於 FCP，root/paint-callback attributes 與 saved theme 一致。這是 browser entry/theme evidence，不是 native first-paint 或 pixel-zero-flash 證明。

Native Full/Lite 以 `ee0f7bb9...` 產物實際啟動，記錄 artifact SHA-256、backend-ready、HTTP/UDP normal 與 fallback dynamic port、entry/guide/theme、HUD 與 listener cleanup。Full normal `8001/8124`、occupier fallback `51604`；Lite normal `8001/8124`、occupier fallback `64746`。Full/Lite artifact SHA 與完整步驟見 [native evidence](g2-native-observations-20260919.md)。因此 C5 在本 closure 的 foundation scope 為 PASS；native screenshot 仍只代表 post-startup observation。

## H5 split evidence

- Browser side：Full/Lite delayed config writes 均在既有 `<2500 ms` timeout 內完成，Full `1814.991 ms`、Lite `1807.562 ms`；兩者在 response 前回到 Live，reentry GET 回讀一致。
- Native side：Full HUD id `83298902`、Lite HUD id `66521214` 各完成兩輪 HUD → Live → HUD，外部 HUD id 維持；`showTeleSuspension` 在 HUD-open 操作中由 `true` 變 `false` 並持續跨 cycle/close，enabled 則在 cycle 期間維持 `true`，只有明確 UI Close 後才變 `false`，兩者均由 GET 確認；theme、fallback 與 listener cleanup 均有實際紀錄。
- 這是 browser delayed-write + native external-HUD lifecycle 的 split H5 evidence。它不聲稱人工 native delay、frame-level zero-flash、所有 external listeners 或 G5 performance；各環境邊界保留於 [browser evidence](g2-browser-observations-20260919.md) 與 [native evidence](g2-native-observations-20260919.md)。H5 以本 G2 foundation scope 核准，後續若需 native pending-write 深測仍應作為獨立 follow-up。

## X1 page-channel evidence

Fresh transparent instrumentation probe 完成三輪 Live → HUD → Live。total active `BroadcastChannel` counts 為 `4,3,4,3,4,3,4`（包含三個 app-global channels）；created totals 為 `7,7,9,9,11,11,13`；first-close call counts 為 `3,4,5,6,7,8,9`。App-global identities 為 ID `1` (`OverlayControlRuntimeProvider`)、ID `4` (`useTelemetry`)、ID `5` (`useOverlayWebSocket`)，各一個 instance；`LiveTelemetryView` identities `7 → 9 → 11 → 13`，temporary StrictMode `6/8/10/12` closed；HUD 沒有新增 page channels，`console.error[]`，tabs closed。

X1 結果只覆蓋本次 browser page-channel probe 的 counts、identity 與 cleanup；不延伸為所有 listener/timer、native resource 或 60 Hz/CPU/RSS/performance G5 evidence。原始 CSV/observer 已保留於 `shell-race-fix/scratch/g2-resource-observer-20260919`。

## Independent review carry-forward

T3 reviewer 確認下列 exact owners 與 generation/archive/save barriers unchanged：`TuneSessionProvider f1f5bd83`、`useEngineMeasurementArchive 966a819`、`engineMeasurementArchive 7f6b3445`、`tuneSessionController 5408d325`、`telemetryCapture 21d74cb4`、`TuningMeasurementStep 8fc186e1`、`TuningTelemetryCaptureView d6cd9b4a`。Observed slip 是 bounded loop 與 tire aggregation 對 contract-valid arrays 的等價結果；Step5 labels/clear confirm 與 lifecycle 無關，custom math engine 不在 capture scope。C3/L3/L4/S1/R2 page identity/current cadence 亦確認 source-reviewed owners unchanged；這些 carry-forward 不代表 calibration、debrief math 或 live FH6 proof。

## Status and handoff

Technical G2 foundation scope is `PASS` after Coordinator approval. G0 observability、G5 real FH6、完整效能與其他 W2–W4 work remain outside this closure. W2 尚未啟動，不發布 `WAVE2_BASE_SHA`；root 完成 final handoff/push 後依使用者條件暫停，保留 `ee0f7bb9...` 作後續 resume candidate。PR #345 可在 root fresh remote checks 後依其非 G5 scope 評估 non-draft，這不等於整體計畫完成。
