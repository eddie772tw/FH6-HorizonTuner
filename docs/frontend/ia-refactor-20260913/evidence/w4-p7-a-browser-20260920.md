# W4-P7-A browser reentry evidence

Date: 2026-09-20 Asia/Taipei  
Candidate before evidence amend: `caa9aa4acadeb41c2592e558fc842f1f37d1f7d5`  
Surface: local Full browser at `http://127.0.0.1:1424/`  
Data source: disconnected backend / default car fixture  
Scope: browser-only; no Tauri/native, real FH6, MoTeC or external HUD claim.

## Steps and observations

1. Started the Vite candidate from the W4 worktree and opened the Full entry. The shell displayed the candidate branch/build label, Live/Tune/Sessions/HUD navigation, Data Out health and backend-disconnected status.
2. Closed the Data Out guide, opened **Tune**, and waited for the lazy workspace. The Tuning Wizard rendered with its four readiness-gated steps, `Review saved runs` action, and the default-car form. This confirms that removing the feature-local `TuneSessionBoundary` did not leave the formal Tune without its root-owned session.
3. Clicked **Review saved runs**. The Road comparison assistant rendered through the root-owned `RoadValidationProvider`; it showed the prepare step, disconnected `Failed to fetch` status, and disabled create action rather than throwing or creating a second controller.
4. Clicked **Return to current setup**. The formal Tune page returned with its step navigation and input state still present. No crash, remount error or lost provider context was observed.

## Result

- Provider boundary cleanup: `pass` for the exercised Full browser path.
- Road review success readback, decision persistence, capture/save ordering and unfinished-workflow preservation: `not-run` because the backend fixture was disconnected.
- Native/Tauri, game telemetry, external MoTeC/HUD and final G5: `not-run` by scope.

The browser server was started only for this evidence and was stopped after the run. The P7-A code and register remain limited to removing redundant fallback wrappers; `reviewHistory` stays registered as a gated compatibility adapter until R1/R3/R4/R5 are proven.

