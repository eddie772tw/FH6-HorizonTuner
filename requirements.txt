# FH6-HorizonTuner 核心執行依賴套件
fastapi>=0.141.1,<1.0.0
uvicorn>=0.52.3,<1.0.0
websockets>=17.0.1,<18.0
pydantic>=2.13.4,<3.0.0
httpx>=0.28.1,<1.0.0
python-multipart
soundcard==0.4.6
# Only the system-media control namespace is needed. These modular packages
# publish CPython 3.13 Windows wheels, avoiding the legacy winsdk sdist build.
# winrt-Windows.Foundation is required to convert WinRT IAsyncOperation objects into Python awaitables.
winrt-Windows.Foundation==3.2.1; sys_platform == 'win32'
winrt-Windows.Media.Control==3.2.1; sys_platform == 'win32'


# 開發與測試工具
ruff
pytest
pytest-asyncio
numpy>=2.5.2
