1. We need to create a new file `tests/test_portable_host_diagnostics.py` to move the tests: `test_executable_bootstrap_and_config_interaction` and `test_portable_executable_releases_udp_port_for_restart` out of `tests/test_executable_bundle.py`.
2. We need to modify `tests/test_portable_host_diagnostics.py` to ensure it captures stdout/stderr when it launches the portable host, and on failure collects:
   - command line
   - executable SHA-256
   - parent PID/exit code
   - backend.log, web_port.txt if present
   - child process tree rooted at the parent PID
   - captured streams
3. We need to create a new GitHub Actions workflow file `.github/workflows/diagnostics.yml` that:
   - Is triggered by `workflow_dispatch` (with a `repeat_count` input) and on a scheduled cadence (e.g., cron).
   - Builds the sidecar once.
   - Builds the Tauri executable once.
   - Runs the host launch probes (the newly moved tests).
   - If tests fail, uploads the diagnostic directory with `actions/upload-artifact`.
4. Modify `tests/test_executable_bundle.py` to remove the two moved tests, leaving only deterministic executable metadata tests.
5. In `.github/workflows/ci.yml`, update the GitHub Actions summary to distinguish blocking package checks from non-blocking host diagnostics.
